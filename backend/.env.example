# Database
DATABASE_HOST=oracle
DATABASE_PORT=1521
DATABASE_USER=aiheatmap
DATABASE_PASSWORD=aiheatmap_dev_123
DATABASE_NAME=XEPDB1

# Redis
REDIS_HOST=redis
REDIS_PORT=6379

# Server
NODE_ENV=development
PORT=3000
LOG_LEVEL=debug

# JWT
JWT_SECRET=your_jwt_secret_key_here
JWT_EXPIRE_IN=7d

# CORS
CORS_ORIGIN=http://localhost:5173

# Email (optional — leave blank to disable; the app no-ops without SMTP)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=you@gmail.com
# SMTP_PASSWORD=your_app_password
# SMTP_FROM=noreply@aiheatmap.com

# External APIs (optional — leave blank to use seeded stock data)
# ALPHA_VANTAGE_API_KEY=
